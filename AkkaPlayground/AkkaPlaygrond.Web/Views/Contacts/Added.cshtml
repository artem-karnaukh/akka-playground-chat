<ion-view view-title="Contact List">
    <ion-content class="padding">
        <ion-list>
            <ion-item class="item-button-right" ng-repeat="user in users" ng-click="openChat(user)">
                {{user.Name}}
            </ion-item>
        </ion-list>
    </ion-content>
</ion-view>